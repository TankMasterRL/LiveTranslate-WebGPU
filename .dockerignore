# Keep the build context small. .model-cache is excluded so the image always
# fetches the Silero model itself (in its own cached layer).
node_modules
.model-cache
build
.svelte-kit
test-results
playwright-report
coverage
.git
*.log
Dockerfile.test
